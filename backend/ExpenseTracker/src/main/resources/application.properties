spring.application.name=ExpenseTracker

spring.datasource.url=jdbc:postgresql://${RENDER_DB_HOST:localhost}:${RENDER_DB_PORT:5432}/${RENDER_DB_NAME:expensetracker}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#email smtp setup
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:sahilmahajan2804@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:fgtl dszu hthf clun}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# File Upload Configuration
file.upload-dir=${FILE_UPLOAD_DIR:uploads}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
# CORS Configuration
ALLOWED_ORIGINS=${ALLOWED_ORIGINS:http://localhost:4200}
